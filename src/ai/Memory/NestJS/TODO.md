TODO: Keep conversation memory isolated from semantic search, vector stores, embedding clients, and embedding indexing.

TODO: .NET is the reference architecture and already implements Redis-backed conversation memory with in-memory fallback.

TODO: Keep this implementation aligned to the same architecture boundaries and observability semantics when parity work is scheduled.